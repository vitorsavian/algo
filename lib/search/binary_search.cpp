// int binarySearch(int list[], int number, int low, int high) {
//     if (list == nullptr) {
//         return 0;
//     }

//     int size = sizeof(list) / sizeof(list[0]);
    
//     if (high >= low) {
//         int mid = low + (high - low) / 2;

//         if (list[mid] == number) {
//             return mid;
//         }

//         if (list[mid] > number) {

//         }
//     }
    
//     return number;

// }

int main(int argc, const char** argv) {
    return 0;
}